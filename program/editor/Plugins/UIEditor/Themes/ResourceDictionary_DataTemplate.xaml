<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:Property="clr-namespace:UIEditor.PropertyGrid"
                    xmlns:Bind="clr-namespace:EditorControlLib.Bind;assembly=EditorControlLib">
    
    <!--<DataTemplate x:Key="UITypesSelect">
        <Property:UIControlTypesSelector ControlType="{Binding Path=Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      BindInstance="{Binding Instance}"
                                      BindProperty="{Binding PGProperty}" />
    </DataTemplate>

    <DataTemplate x:Key="UITemplateSetter">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Property:UIControlTemplateSetter UIControlTemplateId="{Binding Path=Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
            <Bind:BindButton Margin="5,0,0,0" BindInstance="{Binding Instance}" BindProperty="{Binding PGProperty}" Grid.Column="1"/>
        </Grid>
    </DataTemplate>-->
    
</ResourceDictionary>